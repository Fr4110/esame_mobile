name: agenda_legale
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  flutter_secure_storage: ^8.0.0
  local_auth: ^2.1.6
  intl: ^0.18.0
  cupertino_icons: ^1.0.2
  image_picker: ^1.0.2
  table_calendar: ^3.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  # Lasciamo solo questa riga qui sotto per dire che usiamo la libreria
  flutter_launcher_icons: "^0.12.0"

flutter:
  uses-material-design: true

  assets:
    - assets/icona.png
